# TODO

# bugs
- when finishing a configuration, it always appends a "mashups" row to the end of the sequence,
    so when editing an already saved configuration, multiple mashups are at the end
- the transition time is not factored into the sequence times
# features
- 
